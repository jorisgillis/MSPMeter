package ui;

/**
 * The one that provides results of MSP calculations 
 * @author Joris Gillis
 */
public interface ResultProvider {
	
	/**
	 * Register a new listener
	 * @param listener 	the one that's listening
	 */
	public void addResultListener(ResultListener listener);
	
}
